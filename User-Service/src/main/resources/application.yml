server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hisdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

app:
  jwtSecret: ChangeThisSecretToSomethingSecure123!
  jwtExpirationMs: 3600000
