package com.Repository;


import org.springframework.data.jpa.repository.JpaRepository;
import com.Entity.PlanMaster;  // Ensure PlanMaster is Entity not Enum
import org.springframework.data.jpa.repository.JpaRepository;
  

public interface CitizenApplicationRepository extends JpaRepository<User, Integer> {
}






public interface PlanMasterRepository extends JpaRepository<PlanMaster, Integer> {
}

